# Task Management API - Environment Configuration
# Copy this file to .env and fill in actual values

# Database Configuration
# For Neon PostgreSQL, get connection string from https://console.neon.tech
# IMPORTANT: Use postgresql+asyncpg:// (not just postgresql://)
# The application automatically handles sslmode→ssl and removes channel_binding
# You can paste Neon URLs directly (with sslmode and channel_binding)
DATABASE_URL=postgresql+asyncpg://myuser:mypassword@ep-example-123.us-east-2.aws.neon.tech/taskdb?sslmode=require&channel_binding=require

# Logging Configuration
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# API Configuration
# Prefix for task endpoints (default: /tasks)
API_PREFIX=/tasks

# Environment Configuration
# Options: development, staging, production
# In development: prepared statement cache disabled (avoids schema change errors)
# In production: prepared statement cache enabled (better performance)
ENVIRONMENT=development
